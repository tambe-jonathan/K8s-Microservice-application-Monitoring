apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sock-shop-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      name: front-end
  namespaceSelector:
    matchNames:
      - sock-shop
  endpoints:
    - port: http
      interval: 15s
